# Core (analysis + trial generation; CPU, no GPU needed)
numpy>=1.24
matplotlib>=3.7

# Llama-3.1-8B backend (GPU box). transformers 4.x or 5.x both work; the loader
# handles the dtype= / torch_dtype= rename. Install a CUDA-matched torch build.
torch>=2.1
transformers>=4.43
accelerate>=0.30

# Tests
pytest>=7.0
